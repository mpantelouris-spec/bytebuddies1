/** Layout dimensions from product spec */
export const LAYOUT = {
  headerH: 80,
  bottomH: 160,
  leftW: 280,
  rightW: 320,
};

export const VIEWPORT = {
  cameraPos: [0, 0.72, 2.15],
  cameraFov: 48,
  orbitMin: 1.65,
  orbitMax: 4.5,
  heroScale: 2.35,
};

/** Invention workshop — robot closer and larger (hero-first) */
export const WORKSHOP_VIEWPORT = {
  cameraPos: [0, 0.78, 1.75],
  cameraFov: 44,
  orbitMin: 1.35,
  orbitMax: 3.8,
  heroScale: 2.85,
};
