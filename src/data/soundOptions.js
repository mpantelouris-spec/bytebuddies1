import { BLOCK_SOUND_OPTIONS } from '../utils/blockSounds';

/** Blockly FieldDropdown pairs for sound blocks. */
export const SOUND_BLOCKLY_DROPDOWN = BLOCK_SOUND_OPTIONS.map((o) => [o.label, o.id]);
